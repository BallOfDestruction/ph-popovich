@inject Context DbContext
@using App.Models
@model App.Models.ServiceModel

@{
    Layout = null;
}

@if (Model != null)
{
    <figure>
        <img src="@Model.ImageModel.Url" alt="img">
        <figcaption><a href="#"><span>@Html.Raw(Model.About)</span></a></figcaption>
    </figure>
}
