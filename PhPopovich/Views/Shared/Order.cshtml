@using PhPopovich.Models
@inject Context DbContext
@model PhPopovich.Controllers.OrderViewModel

@{
    Layout = null;
}

<div class="container" id="order">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <h3 class="title">Заказать фотосессию</h3>
            <form asp-action="MakeOrder" asp-controller="Order" method="post" enctype="multipart/form-data">
                <div class="form-group">
                    @Html.LabelFor(model => model.Name)
                    @Html.EditorFor(model => model.Name, new {htmlAttributes = new 
                    {
                        @class = "form-control",
                        @placeholder = "Имя фамилия"}
                    })
                    @Html.ValidationMessageFor(model => model.Name, "", new {@class = "text-danger"})
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.EmailOrPhone)
                    @Html.EditorFor(model => model.EmailOrPhone, new {htmlAttributes = new
                    {
                        @class = "form-control",
                        @placeholder = "Email или телефон"
                    }})
                    @Html.ValidationMessageFor(model => model.EmailOrPhone, "", new {@class = "text-danger"})
                </div>
                
                <div class="form-group">
                    @Html.LabelFor(model => model.DesireDate)
                    @Html.EditorFor(model => model.DesireDate, new {htmlAttributes = new {@class = "form-control"}})
                    @Html.ValidationMessageFor(model => model.DesireDate, "", new {@class = "text-danger"})
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.About)
                    @Html.EditorFor(model => model.About, new {htmlAttributes = new
                    {
                        @class = "form-control",
                        @placeholder = "Чтобы вы хотели"
                    }})
                    @Html.ValidationMessageFor(model => model.About, "", new {@class = "text-danger"})
                </div>

                <div class="form-group text-center">
                    <button type="submit">Заказать</button>
                </div>
                
            </form>
        </div>
    </div>
</div>